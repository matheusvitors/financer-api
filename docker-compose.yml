services:
  mysql:
    image: mysql:latest
    container_name: mysql-container
    environment:
      - MYSQL_ROOT_PASSWORD=123456
      - MYSQL_DATABASE=financer_test
    ports:
      - "3306:33070nz"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
